let arr = [1, 2, 3, 4, 5, 6, 7, 8]
const addUp = (arr, num) => {
	let counter = 0
	let container = []
	let difference = 0

	// for (let i = 0; i <= arr.length; i++) {
	// 	if (arr[i] + arr[i + 1] === num) {
	// 		container.push(arr[i], arr[i + 1])
	// 		counter++
	// 	}
	// }
	return container
	// for (let i = 0; i < arr.length; i++) {
	// 	for (let j = i + 1; j < arr.length; j++) {
	// 		if (arr[i] + arr[j] === num) {
	// 			return arr[i] + ' ' + arr[j]
	// 		}
	// 	}
	// }
	// return -1
}
console.log(addUp(arr, 12))
